<% provide(:title, 'All Cards') %>
<h1>All cards</h1>

<ul class="cards">
  <% @cards.each do |card| %>
    <li>
      <p><%= card.title %></p>
      <p><%= card.description %></p>
    </li>
  <% end %>
</ul>
